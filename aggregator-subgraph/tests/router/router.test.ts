import { test, describe, assert } from 'matchstick-as/assembly/index'
import { handleExchange } from '../../src/router/router'
import { createRouterExchangeEvent } from './utils'

describe('test router', () => {
	test('should new exchange', () => {
		// assert.entityCount('Exchange', 0)	
		// let exchangeEvent = createRouterExchangeEvent()
		// handleExchange(exchangeEvent)
	}),

	test('handle swapped', () => {

	}),

	test('handle client data', () => {

	})
}) 
